describe('unit tests html snapshots', function () {
    describe('take', function () {
        it('should return the html from a valid url', function () {
            //TODO.. Need to test that phantomjs is called correctly
        });

        it('should return an InvalidUrlError if an invalid url is provided', function () {
            //TODO.. Need to test that phantomjs is called correctly
        });
    });
});
